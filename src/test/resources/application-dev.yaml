grpcserver:
  port: 0

webserver:
  port: 0

pgpool:
  connect-options:
    port: 15432
    host: localhost
    database: catalog
    user: catalog
    password: catalog
  pool-options:
    max-size: 5

flyway:
  url: jdbc:postgresql://localhost:15432/catalog
  user: catalog
  password: catalog

tracing:
  enabled: false

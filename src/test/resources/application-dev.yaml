grpcserver:
  port: 0

webserver:
  port: 0

pgpool:
  connect-options:
    port: 10112
    host: localhost
    database: catalog
    user: catalog
    password: catalog
  pool-options:
    max-size: 5

init:
  db-connectivity-attempts: 30

flyway:
  url: jdbc:postgresql://localhost:10112/catalog
  user: catalog
  password: catalog

tracing:
  enabled: false

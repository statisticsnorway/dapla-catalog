grpcserver:
  port: 0

webserver:
  port: 0

pgpool:
  connect-options:
    port: 10112
    host: postgres
    database: catalog
    user: catalog
    password: catalog
  pool-options:
    max-size: 5

init:
  db-connectivity-attempts: 3

flyway:
  url: jdbc:postgresql://postgres:10112/catalog
  user: catalog
  password: catalog

tracing:
  enabled: false

pubsub:
  enabled: false